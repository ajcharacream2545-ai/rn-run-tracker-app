export type Runs = {
  id: string; // เปลี่ยนจาก number เป็น string ให้ตรงกับโครงสร้าง FlatList
  location: string;
  distance: number;
  time_of_day: string;
  run_date: string;
  image_url: string;
};
